docker run -d -it --name ubuntu ubuntu
docker exec ubuntu bash -c "apt update && apt install -y curl"
docker exec -it ubuntu sh -c 'echo "Input website:"; read website; echo "Searching.."; sleep 1; curl http://$website;'
